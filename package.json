{
  "name": "rd-mart-api",
  "version": "1.0.0",
  "private": true,
  "description": "Serverless API functions for RD MART (Vercel). The static app itself lives in martai_final/ and has no build step.",
  "scripts": {
    "start": "node martai_final/server.js",
    "dev": "node martai_final/server.js",
    "test": "npm --prefix martai_final test"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  }
}
